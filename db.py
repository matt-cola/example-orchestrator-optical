"""Database models of the example optical orchestrator.

This example adds no custom tables of its own: the orchestrator-optical module owns no
tables either, everything lives in the orchestrator-core catalog tables.
"""
